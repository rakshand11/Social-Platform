// Basic of Javascripts

const decoded = {
  userId: "huhu",
  iat: 667788,
};

console.log(decoded.userId);

// if i want to excess inside value of decoded which is an object than i have to write decoded.userId
